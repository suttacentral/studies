/**
 * To split a date into days, months, years, etc
 *
 * @param {string} dateAsString
 * @returns {ArrayLike.<string> | null}
 */
export function splitDate(dateAsString: string): ArrayLike<string> | null;
